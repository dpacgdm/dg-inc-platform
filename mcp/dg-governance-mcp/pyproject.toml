[project]
name = "dg-governance-mcp"
version = "0.1.0"
description = "D&G Inc platform governance MCP server"
requires-python = ">=3.11"
dependencies = [
  "mcp[cli]>=1.9.0",
  "pydantic>=2.7.0"
]

[tool.ruff]
line-length = 100

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
